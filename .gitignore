# Local npm install state (this project has no runtime npm dependencies)
node_modules/
.npm/
npm-debug.log*
package-lock.json

# Generated data-loader output (snapshots, variants, logs) — not source
build/
